This is going to be an erlang server that relays message based on who can hear them.

I am at x, y.

/whisper
/say
/yell
/shout


Running
-------
This runs a server that clients (like hla-client) can talk to:

    erl -compile *.erl 
    erl -noshell -s network.erl main -s init stop<CR>
